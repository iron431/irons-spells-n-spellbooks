package io.redspace.ironsspellbooks.spells.evocation;import io.redspace.ironsspellbooks.api.config.DefaultConfig;import io.redspace.ironsspellbooks.api.registry.SchoolRegistry;import io.redspace.ironsspellbooks.api.spells.AbstractSpell;import io.redspace.ironsspellbooks.api.spells.SpellRarity;import io.redspace.ironsspellbooks.api.util.Utils;import io.redspace.ironsspellbooks.capabilities.magic.MagicManager;import io.redspace.ironsspellbooks.damage.DamageSources;import io.redspace.ironsspellbooks.effect.SlowedEffect;import io.redspace.ironsspellbooks.registries.MobEffectRegistry;import io.redspace.ironsspellbooks.util.ParticleHelper;import io.redspace.skillcasting.data.CastContext;import io.redspace.skillcasting.data.cast.CastType;import io.redspace.skillcasting.data.PlayableSound;import io.redspace.skillcasting.registry.SkillcastingComponentTypes;import io.redspace.skillcasting.util.SkillcastingUtils;import net.minecraft.network.chat.Component;import net.minecraft.network.chat.MutableComponent;import net.minecraft.server.level.ServerLevel;import net.minecraft.sounds.SoundEvents;import net.minecraft.world.effect.MobEffectInstance;import net.minecraft.world.entity.Entity;import net.minecraft.world.entity.LivingEntity;import org.joml.Vector3f;import java.util.List;import java.util.Optional;public class SlowSpell extends AbstractSpell {    @Override    public List<MutableComponent> getUniqueInfo(CastContext castContext) {        return List.of(                Component.translatable("ui.irons_spellbooks.slowed",                        Utils.stringTruncation(SlowedEffect.getPercentForAmplifier(castContext.getOrDefault(SkillcastingComponentTypes.EFFECT_AMPLIFIER, 0)) * 100, 1)),                Component.translatable("ui.irons_spellbooks.effect_length",                        Utils.timeFromTicks(castContext.getOrDefault(SkillcastingComponentTypes.EFFECT_DURATION_TICKS, 0), 1))        );    }    private final DefaultConfig defaultConfig = new DefaultConfig()            .setMinRarity(SpellRarity.EPIC)            .setSchoolResource(SchoolRegistry.EVOCATION_RESOURCE)            .setMaxLevel(4)            .setCooldownSeconds(80)            .build();    public SlowSpell() {        this.manaCostPerLevel = 10;        this.baseSpellPower = 20;        this.spellPowerPerLevel = 4;        this.castTime = 30;        this.baseManaCost = 50;    }    @Override    public CastType getCastType() {        return CastType.LONG;    }    @Override    public DefaultConfig getDefaultConfig() {        return defaultConfig;    }    @Override    public Optional<PlayableSound> getCastStartSound(CastContext castContext) {        return PlayableSound.standard(SoundEvents.EVOKER_PREPARE_SUMMON).toOpt();    }    @Override    public void buildContextComponents(CastContext castContext) {        super.buildContextComponents(castContext);        castContext.set(SkillcastingComponentTypes.CAST_RANGE, 32f);        castContext.set(SkillcastingComponentTypes.EFFECT_DURATION_TICKS, (int) (getSpellPower(castContext) * 20));        int base = 8 - 1; // 20%        castContext.set(SkillcastingComponentTypes.EFFECT_AMPLIFIER, (int) (base * getSpellPowerMultiplier(castContext)));    }    @Override    public boolean checkPreCastConditions(CastContext castContext) {        Entity caster = castContext.asEntityCaster();        return SkillcastingUtils.preCastTargetHelper(castContext,                castContext.getOrDefault(SkillcastingComponentTypes.CAST_RANGE, 32f).intValue(), 0.35f, true,                target -> target != caster && !DamageSources.isFriendlyFireBetween(caster, target));    }    @Override    public void onCast(ServerLevel level, CastContext castContext) {        if (!(level instanceof ServerLevel serverLevel)) {            return;        }        var targetData = castContext.getOrNull(SkillcastingComponentTypes.TARGETED_ENTITIES);        LivingEntity targetEntity = targetData != null ? targetData.getFirstLivingEntityTarget(serverLevel) : null;        if (targetEntity == null) {            return;        }        int duration = castContext.getOrDefault(SkillcastingComponentTypes.EFFECT_DURATION_TICKS, 0);        int amplifier = castContext.getOrDefault(SkillcastingComponentTypes.EFFECT_AMPLIFIER, 0);        targetEntity.addEffect(new MobEffectInstance(MobEffectRegistry.SLOWED, duration, amplifier, false, false, true));        MagicManager.spawnParticles(level, ParticleHelper.UNSTABLE_ENDER, targetEntity.getX(), targetEntity.getY() + .25, targetEntity.getZ(), 15, targetEntity.getBbWidth() * 0.5, targetEntity.getBbWidth() * 0.5, targetEntity.getBbWidth() * 0.5, 0, false);    }    @Override    public Vector3f getAccentColor() {        return Utils.deconstructRGB(MobEffectRegistry.SLOWED.get().getColor());    }}